attributes_table_for opco do
  row :nom
  row :nom_parcours_type do |opco|
    noms = suggestions_nom_parcours_type_evapro(opco)
    noms.map { |nom| tag.code(nom) }.join(tag.br).html_safe
  end
  row :financeur
  row :logo do
    if opco.logo.attached?
      link_to cdn_for(opco.logo), target: "_blank", rel: "noopener" do
        image_tag(cdn_for(opco.logo.variant(:defaut)),
                  alt: opco.logo.filename,
                  class: "image-preview")
      end
    else
      span "Aucun logo"
    end
  end
  row :financeur do |opco|
    badge_financeur(opco)
  end
  row :telephone
  row :email
  row :url
  row :url_contact
  row :idcc do |opco|
    if opco.idcc.any?
      opco.idcc.join(", ")
    else
      "Aucun IDCC"
    end
  end
  row :created_at
end
