<% content_for :classes_body, "page_connexion_v2" %>
<% content_for :classes_conteneur, " connexion_v2" %>
<%= render HeaderComponent.new(
  titre: t("header.titre"),
  tagline: t("header.tagline"),
  logged_in: false,
  show_navigation: false
) %>
<% content_for :content_block do %>
  <div class="connexion_v2__content">
    <div class="connexion_v2__content__inner">
      <div id="active_admin_content">
        <%= yield %>
      </div>
    </div>
  </div>
<% end %>

<%= render partial: "layouts/active_admin_logged_out_structure" %>
