# frozen_string_literal: true

panel 'Synthèse' do
  h3 evaluation_globale.utilisateur
  h4 l evaluation_globale.date
  h2 "Efficience globale : #{en_pourcentage(evaluation_globale.efficience)}"

  attributes_table_for evaluation_globale.evaluations do
    row :situation
    row(:efficience) { |e| en_pourcentage(e.efficience) }
  end
end

evaluation_globale.evaluations.each do |evaluation|
  panel evaluation.situation do
    render evaluation.situation, evaluation: evaluation
    render 'admin/evaluations/evaluation_competences', evaluation: evaluation
  end
end
